# פורמט ייבוא רווחית — ממצאים מהקובץ שנשלח (`NewItemsToRivhitSS24.xls`)

קובץ `.xls` ישן (legacy Excel), גיליון בודד, **בלי שורת כותרות** — 11 עמודות
קבועות, שורה אחת לכל שילוב פריט+מידה+צבע:

| עמודה | תוכן | דוגמה |
|---|---|---|
| A | מספר פריט רץ ברווחית | `4713` |
| B | תיאור: `{שם המוצר}, {מידה}, {צבע}, Item {מק"ט יצרן}` | `Bomber Jacket, S, Slate Brown, Item 35101.240078` |
| C | ברקוד יצרן | `8715738826175` |
| D–J | תמיד ריקות (7 עמודות) | — |
| K | מחיר (מספר בודד) | `574` |

נבדק על 4173 שורות מהקובץ ששלחת — כל הממצאים הבאים מאומתים אוטומטית
בבדיקות (`tests/test_rivhit_format.py`):

- **עמודה A רציפה לחלוטין, בלי שום פער**, מ-4713 עד 8885. זה אומר שזה
  מונה שאתה/אבא שלך מנהלים ידנית ומגדילים אחד-אחד בכל קובץ חדש שנשלח
  לרווחית — **ולא** שדה שרווחית ממלאת אוטומטית. כדי לייצר קובץ תקין
  לעונה חדשה, האפליקציה תצטרך לדעת מאיפה להמשיך (המספר הבא = המספר
  הגבוה ביותר בקובץ הקודם + 1). אני כבר בניתי פונקציה שמחשבת את זה
  אוטומטית אם תיתן לה את הקובץ האחרון שנשלח לרווחית.
- **עמודה B** נבנית מ-4 שדות שכולם כבר קיימים בקובץ היצרן (Item
  Description, Size, Color Description, Item code) — כתבתי פונקציה
  שמרכיבה את המחרוזת בדיוק באותו פורמט, ואימתתי מול הקובץ האמיתי.
- נמצאה שורה אחת ש"זבל"/הערה בתוך הנתונים ולא פריט אמיתי (שורה 3018:
  "SWIMSUITS & BIKINIS CAN BE FOUND ON THE LIFESTYLE SS2024 ORDERFORM!").
  הפרסר מזהה שורות כאלה (`is_valid=False`) במקום לזרוק אותן בשקט, כדי
  שאפשר יהיה להציג אזהרה למשתמש בממשק.

## עדיין פתוח — צריך תשובה ממך/מאבא

1. **עמודה K (מחיר)** — איזה שדה מקובץ היצרן זה בדיוק? מחיר קמעונאי
   (Suggested Retail Price) או מחיר סיטונאי/עלות (USDEX Price)? ובאיזה
   מטבע רווחית מצפה למחיר הזה — דולר כמו שמופיע בקובץ היצרן, או שקל
   (ואז צריך גם שער המרה)?
2. **עמודות D–J** — תמיד ריקות בקובץ שקיבלתי. זה שדות שרווחית פשוט לא
   דורשת בייבוא הזה (אז נשאיר ריק גם אנחנו — זה מה שהאפליקציה עושה כרגע
   כברירת מחדל), או שיש שם משהו שפשוט לא מולא בעבר ואולי כדאי למלא
   (כמו קטגוריה, ספק, כמות במלאי)?
3. **מספר הפריט ההתחלתי** — כדי לחשב את המספר הבא לעונה הנוכחית (SS26),
   אני צריך את **הקובץ האחרון שבאמת נשלח לרווחית** (לא בהכרח קובץ ה-SS24
   ששלחת עכשיו — אם יש קובץ מאוחר יותר, זה מה שקובע את המספר הבא).
